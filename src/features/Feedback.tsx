import { Fragment } from 'react';

export const Feedback = () => {
  return (
    <Fragment>
      {/*<Form.Item name='cost1' label='Расценка 1'>*/}
      {/*<Input placeholder='Введите расценку'></Input>*/}
      {/*</Form.Item>*/}
      1. Добавление заголовка (текст без ограничений, лимит 50 символов) 2.
      Добавление текста отзыва (текст без ограничений, лимит 250 символов) 3.
      Добавление подписи (текст без ограничений, лимит 50 символов)
    </Fragment>
  );
};
